# my_local_package/setup.py
from setuptools import setup, find_packages

setup(
    name='morphomics_exp',
    version='0.1',
    packages=find_packages(),
    install_requires=[],  # list dependencies if any
)